server:
  port: 8086

spring:
  application:
    name: resource-service
  datasource:
    url: jdbc:postgresql://localhost:5432/healthcare
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
