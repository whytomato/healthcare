# Domain Docs

This is a single-context repo.

Before exploring, engineering skills should read:

- `CONTEXT.md` at the repo root, if it exists
- `docs/adr/`, if it exists

If these files do not exist, proceed silently. The producer skill creates them later when useful.
